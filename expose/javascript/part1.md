# Part 1. A Quick Introduction
1. 20
2. 20
3. You should not use the var keyword because it can be accessed anywhere inside the function it is defined in. This leads to naming conflicts and scoping issues.
4. 20
5. The code returns an error because the content is saved into the memory area associated with the variable. Line 13 is not within the same scope so the code throws an error.
6. 20
7. The code returns an error because the content is saved into the memory area associated with the variable. Line 13 is not within the same scope so the code throws an error.
